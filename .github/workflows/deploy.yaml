name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    name: Test
    uses: noyabronok/vite-ts-react/.github/workflows/test.yaml@main
  deploy:
    name: Deploy
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 4
    env:
      MY_EMAIL: testemail@gmail.com
      MY_NAME: Noyabronok
    steps:
      - name: ☁️ checkout repository
        uses: actions/checkout@v3

      - name: 🔧 setup node 18.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.x

      - name: 📦 install dependencies
        run: npm ci

      - name: 🚀 deploy
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email $MY_EMAIL
          git config --global user.name $MY_NAME
          git remote set-url origin https://$MY_NAME:${{ secrets.DEPLOY_TOKEN }}@github.com/noybronok/vite-ts-react.git
          npm run deploy
